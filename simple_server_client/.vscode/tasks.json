{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Simple Server",
            "type": "shell",
            "command": "cd ${workspaceFolder}/simple_server && cmake . && make",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}
